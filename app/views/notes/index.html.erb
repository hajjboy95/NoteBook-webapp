<h1>Index PAth</h1>
<% @notes.each do |note|%>

    <h3><%= link_to note.title , note_path(note)  %></h3>
    <p><%= time_ago_in_words(note.created_at) %></p>
<%end %>


<%= link_to "New Note" , new_note_path %>
